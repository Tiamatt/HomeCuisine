import { NullOrWhiteSpaceValidatorDirective } from './null-or-white-space-validator.directive';

describe('NullOrWhiteSpaceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NullOrWhiteSpaceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
